{
  "name": "lanyell-app",
  "version": "0.2.0",
  "private": true,
  "description": "lanyell desktop app (Tauri shell around the lanyell server sidecar)",
  "scripts": {
    "build:ui": "esbuild ui/main.js --bundle --format=iife --outfile=ui/bundle.js",
    "build:sidecar": "node build-sidecar.js",
    "build:mac": "npm run tauri -- build --bundles app && hdiutil create -volname lanyell -srcfolder src-tauri/target/release/bundle/macos/lanyell.app -ov -format UDZO src-tauri/target/release/bundle/macos/lanyell.dmg",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "qrcode": "^1.5.4"
  }
}
